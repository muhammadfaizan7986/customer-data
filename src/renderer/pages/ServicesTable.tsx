import React, { Fragment } from 'react';
import ServicesTablePage from '../modules/ServicesTablePage';

type Props = {};

const ServicesTable = (props: Props) => {
  return (
    <Fragment>
      <ServicesTablePage />
    </Fragment>
  );
};

export default ServicesTable;
