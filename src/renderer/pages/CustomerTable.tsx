import React, { Fragment } from 'react';
import CustomerTablePage from '../modules/CustomerTablePage';

type Props = {};

const CustomerTable = (props: Props) => {
  return (
    <Fragment>
      <CustomerTablePage />
    </Fragment>
  );
};

export default CustomerTable;
