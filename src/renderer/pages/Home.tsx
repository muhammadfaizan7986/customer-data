import React, { Fragment } from 'react';
import HomePage from '../modules/HomePage';

type Props = {};

const Home = (props: Props) => {
  return (
    <Fragment>
      <HomePage />
    </Fragment>
  );
};

export default Home;
